# GitHub Personal Access Token
# Required for authenticating with GitHub's API
# Should have permissions for:
# - repo (Full control of private repositories)
# - admin:org (Full control of orgs and teams)
# - workflow (Update GitHub Action workflows)
GITHUB_TOKEN=your_github_token_here

# GitHub Organization Name
# The name of your GitHub organization where the CAP repositories are hosted
# Example: if your repos are at github.com/my-org/cap-cloud-ce, use "my-org"
ORGANIZATION=your_organization_name

# Optional: Custom API URL (only needed for GitHub Enterprise)
# GITHUB_API_URL=https://github.mycompany.com/api/v3

# Optional: Logging Level
# Valid values: debug, info, warn, error
# LOG_LEVEL=info